services:
  converter:
    build: .
    container_name: media_converter
    volumes:
      - ./media:/app/media
      - ./output:/app/output
      - ./settings.toml:/app/settings.toml:ro
      - ./logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
